## Packages
axios | HTTP client for external API requests
recharts | Fintech data visualization and charts
framer-motion | Smooth animations and transitions
date-fns | Date formatting for transaction tables
clsx | Utility for conditional class names
tailwind-merge | Utility for merging tailwind classes

## Notes
The application connects to an external backend defined by VITE_API_BASE_URL.
Dark mode is the default and enforced theme for the fintech aesthetic.
